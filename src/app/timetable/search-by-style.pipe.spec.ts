import { SearchByStylePipe } from './search-by-style.pipe';

describe('SearchByStylePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByStylePipe();
    expect(pipe).toBeTruthy();
  });
});
